name: lifetrace_assets
description: Local-first personal asset lifecycle application for LifeTrace.
publish_to: "none"
version: 0.2.0+2

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.5
  sembast: ^3.8.10
  sembast_web: ^2.4.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
